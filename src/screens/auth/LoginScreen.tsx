import LoginCard from '../../components/LoginCard';

export default function LoginScreen() {
    return (
        <div className="container" style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', backgroundColor: '#f0f0f0', minHeight: '100vh' }}>
            <LoginCard />
        </div>
    );
} 