name: "Create Release and Publish Android Package"

on:
  pull_request:
    types: [ closed ]
    branches: [ main, dev ]

jobs:
  create_release:
    name: Create Git Tag and Release
    runs-on: ubuntu-latest
    # Only run if the PR was merged (not just closed)
    if: github.event.pull_request.merged == true
    outputs:
      version: ${{ steps.read_version.outputs.version }}
      version_type: ${{ steps.version_type.outputs.type }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          # Fetch full history for proper versioning
          fetch-depth: 0
          # Use a token with write permissions to create tags
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Grant execute permission for update_version.sh
        run: chmod +x ./update_version.sh

      - name: Determine version bump type
        id: version_type
        run: |
          # Get PR labels and title
          PR_LABELS='${{ toJson(github.event.pull_request.labels.*.name) }}'
          PR_TITLE='${{ github.event.pull_request.title }}'
          echo "PR Labels: $PR_LABELS"
          echo "PR Title: $PR_TITLE"
          
          # Default to patch if no specific label or title keyword is found
          VERSION_TYPE="patch"
          
          # Check PR title first (case-insensitive)
          if echo "$PR_TITLE" | grep -qi "^major\|^breaking\|\[major\]\|\[breaking\]"; then
            VERSION_TYPE="major"
          elif echo "$PR_TITLE" | grep -qi "^minor\|^feature\|^enhancement\|\[minor\]\|\[feature\]\|\[enhancement\]"; then
            VERSION_TYPE="minor"
          elif echo "$PR_TITLE" | grep -qi "^patch\|^bugfix\|^hotfix\|^fix\|\[patch\]\|\[bugfix\]\|\[hotfix\]\|\[fix\]"; then
            VERSION_TYPE="patch"
          # If no title keywords found, check labels (priority: major > minor > patch)
          elif echo "$PR_LABELS" | grep -qi "major\|breaking"; then
            VERSION_TYPE="major"
          elif echo "$PR_LABELS" | grep -qi "minor\|feature\|enhancement"; then
            VERSION_TYPE="minor"
          elif echo "$PR_LABELS" | grep -qi "patch\|bugfix\|hotfix"; then
            VERSION_TYPE="patch"
          fi
          
          echo "Version bump type: $VERSION_TYPE"
          echo "type=$VERSION_TYPE" >> $GITHUB_OUTPUT

      - name: Update version
        run: ./update_version.sh ${{ steps.version_type.outputs.type }}

      - name: Commit and Push changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Update VERSION file"
          branch: ${{ github.ref_name }}

      - name: Read VERSION file
        id: read_version
        run: |
          VERSION=$(cat VERSION)
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Create Git Tag
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git tag -a "v${{ env.VERSION }}" -m "Release version ${{ env.VERSION }}"
          git push origin "v${{ env.VERSION }}"

      - name: Create GitHub Release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: "v${{ env.VERSION }}"
          release_name: "Release v${{ env.VERSION }}"
          body: |
            ## Changes in this Release
            
            **Version Bump**: ${{ steps.version_type.outputs.type }}
            
            This release was automatically created from PR #${{ github.event.pull_request.number }}: ${{ github.event.pull_request.title }}
            
            ### PR Labels Used for Versioning:
            ${{ join(github.event.pull_request.labels.*.name, ', ') }}
            
            **Full Changelog**: https://github.com/${{ github.repository }}/compare/v${{ steps.read_version.outputs.version }}...v${{ env.VERSION }}
          draft: false
          prerelease: false

#  Verify if this is the way to create apks.
#  publish_android_package:
#    name: Build and Publish Android Package
#    runs-on: ubuntu-latest
#    needs: create_release # Ensures this job runs after create_release
#    if: github.event.pull_request.merged == true # Only runs if the PR was merged
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v3
#
#      - name: Set up Node.js
#        uses: actions/setup-node@v3
#        with:
#          node-version: '18' # Specify your Node.js version
#
#      - name: Set up JDK # Although EAS Build is cloud-based, this might be useful for local tools or if 'ejected'
#        uses: actions/setup-java@v3
#        with:
#          distribution: 'temurin' # Or any other JDK distribution you prefer
#          java-version: '17' # Or the JDK version required by your project
#
#      - name: Install project dependencies
#        run: npm install # Or yarn install if you use yarn
#
#      - name: Install EAS CLI
#        run: sudo npm install -g eas-cli
#
#      # EAS CLI will automatically use EXPO_TOKEN from the environment for authentication.
#      # No explicit 'eas login' step is needed if EXPO_TOKEN is set.
#
#      - name: Build Android APK with EAS Build
#        env:
#          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
#          # Optional: If your project is not linked or to avoid ambiguity,
#          # you can set EAS_PROJECT_ID as a secret and uncomment the line below:
#          # EAS_PROJECT_ID: ${{ secrets.EAS_PROJECT_ID }}
#        run: |
#          eas build -p android --profile preview --non-interactive --wait --output=./app-release.apk
#          echo "EAS Build finished. APK expected at ./app-release.apk"
#        # Note: You must have a profile named 'preview' in your eas.json.
#        # If your build profile for APKs is named differently (e.g., 'production'),
#        # update '--profile preview' to '--profile production' (or your profile's name).
#
#      - name: Verify APK was created # Optional step for debugging
#        run: |
#          echo "Listing files in current directory:"
#          ls -la
#          if [ ! -f ./app-release.apk ]; then
#            echo "Error: APK file (./app-release.apk) not found after EAS build!"
#            exit 1
#          fi
#          echo "APK file found: ./app-release.apk"
#
#      - name: Upload APK to GitHub Release
#        uses: actions/upload-release-asset@v1
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#        with:
#          upload_url: ${{ needs.create_release.outputs.upload_url }} # Upload URL from the created release
#          asset_path: ./app-release.apk # Updated path to the APK generated by EAS Build
#          asset_name: ${{ github.event.repository.name }}-${{ needs.create_release.outputs.version }}.apk # APK filename
#          asset_content_type: application/vnd.android.package-archive
